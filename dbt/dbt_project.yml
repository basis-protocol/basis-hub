name: 'basis_discovery'
version: '0.1.0'
config-version: 2

profile: 'basis'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  basis_discovery:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
      +schema: discovery
    discovery:
      +materialized: table
      +schema: discovery
